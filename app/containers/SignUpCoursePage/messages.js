import { defineMessages } from 'react-intl';

const account = 'account';

export default defineMessages({
  goToProject: {
    id: `${account}.goToProject`,
    defaultMessage: 'Перейти в проект',
  },
});
